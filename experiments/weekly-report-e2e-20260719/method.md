# Method

1. Run YAML validation and the unit tests.
2. Run `make report` from `weekly-report/`.
3. Render every output-PDF page to PNG and inspect it for visual defects.
4. Make only the smallest change needed if a defect is observed, then rebuild and re-inspect.
