# Validation

- `make check` and `make test` exit successfully.
- `make report` exits successfully and writes `weekly-report.pdf`.
- Page images show readable text, correctly loaded images, stable headers/footers, and no clipping or overlap.
